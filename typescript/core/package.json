{
  "name": "@4dgs/core",
  "version": "0.6.0",
  "description": "The 4dgs decoder and encoder. No I/O of its own: reads through an IReadable, so the same code runs in a browser, on a server, and over a byte array in a test.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/avala-ai/4dgs.git"
  },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "homepage": "https://4dgs.dev"
}
