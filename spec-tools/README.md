# spec-tools

**Planned.** A generator that turns the specification's record definitions into the field tables in
the spec and the record structs in each SDK, so both come from one source and cannot drift.

Today the tables in `website/docs/spec/index.md` are maintained by hand and the SDKs mirror them by
review.
